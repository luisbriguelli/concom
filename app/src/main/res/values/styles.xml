<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@android:color/white</item>
    </style>

    <style name="Theme.MainTheme.HotelsList" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/grey_700</item>
        <item name="colorAccent">@color/grey_700</item>
        <item name="android:textColorPrimary">@color/grey_700</item>
        <item name="colorControlNormal">@color/grey_700</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:textColorHint">@color/white_transparency</item>
    </style>

</resources>
